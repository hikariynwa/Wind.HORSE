<% flash.each do |message_type, message| %>
  <div class="<%= message_type == "notice" ? "bg-green-500" : message_type == 'alert' ? 'bg-red-500' : 'bg-gray-500' %> text-white px-4 py-2 rounded-md mb-4 %>">
    <%= message %>
  </div>
<% end %>